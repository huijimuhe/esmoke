<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/background.main</item>
        <item name="colorPrimaryDark">@color/background.main</item>
        <item name="colorAccent">@color/background.fire</item>
        <item name="android:colorBackground">@color/background.blank</item>
    </style>

    <style name="Toolbar.Main" parent="Base.ThemeOverlay.AppCompat.ActionBar">
        <item name="colorPrimary">@color/background.blank</item>
        <item name="colorPrimaryDark">@color/background.main</item>
    </style>

    <style name="Menu.Sub" parent="TextAppearance.AppCompat">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text.sub</item>
        <item name="android:drawablePadding">3dp</item>
        <item name="android:paddingBottom">30dp</item>
        <item name="android:paddingTop">6dp</item>
    </style>

    <style name="Menu.Main" parent="TextAppearance.AppCompat">
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/text.main</item>
        <item name="android:drawablePadding">3dp</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="Menu.Cover" parent="TextAppearance.AppCompat">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#464646</item>
        <item name="android:drawablePadding">3dp</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
